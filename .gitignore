# 빌드 임시 산출물만 제외 — .exe + DLL은 git에 포함 (다른 PC에서 컴파일러 없이 실행용)
build/*.obj
build/*.o
build/*.txt
build/hello.*
*.o
_dbg_*

# 사운드: 원본(sounds/)만 커밋, build로 복사된 건 제외 (build.bat가 재생성)
build/sounds/

# python 캐시 (사운드 합성기 tools/synth_audio.py)
__pycache__/
*.pyc

# editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
